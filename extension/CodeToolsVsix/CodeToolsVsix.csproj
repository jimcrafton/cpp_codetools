<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net472</TargetFramework>
    <RootNamespace>CodeToolsVsix</RootNamespace>
    <AssemblyName>CodeToolsVsix</AssemblyName>

    <!-- VSIX / VS package plumbing -->
    <GeneratePkgDefFile>true</GeneratePkgDefFile>
    <IncludeAssemblyInVSIXContainer>true</IncludeAssemblyInVSIXContainer>
    <IncludeDebugSymbolsInVSIXContainer>false</IncludeDebugSymbolsInVSIXContainer>
    <IncludeDebugSymbolsInLocalVSIXDeployment>false</IncludeDebugSymbolsInLocalVSIXDeployment>
    <CopyBuildOutputToOutputDirectory>true</CopyBuildOutputToOutputDirectory>
    <CopyOutputSymbolsToOutputDirectory>true</CopyOutputSymbolsToOutputDirectory>
    <StartAction>Program</StartAction>
    <StartProgram Condition="'$(DevEnvDir)' != ''">$(DevEnvDir)devenv.exe</StartProgram>
    <StartArguments>/rootsuffix Exp</StartArguments>
    <UseCodebase>true</UseCodebase>

    <!-- In-proc VS packages that implement legacy COM editor interfaces (IVsEditorFactory,
         IVsWindowPane) must be .NET Framework - the shell loads them in-proc, not OOP. -->
    <UseWindowsForms>false</UseWindowsForms>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <Platforms>AnyCPU;x64</Platforms>
    <ExtensionInstallationRoot>CommonExtensions</ExtensionInstallationRoot>
    <ExtensionInstallationFolder>Microsoft\CodeToolsVsix</ExtensionInstallationFolder>

    <MinimumVisualStudioVersion>17.0</MinimumVisualStudioVersion>

    <!-- Microsoft.VSSDK.BuildTools ships its own self-contained VsSDK.targets/toolchain under
         its NuGet package (tools\VSSDK\), so building doesn't require Visual Studio to be
         installed on the build machine - but that import is opt-in and skipped unless this is
         set. -->
    <VSSDKBuildToolsAutoSetup>true</VSSDKBuildToolsAutoSetup>

    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
    <GenerateAssemblyInfo>true</GenerateAssemblyInfo>
    <Deterministic>true</Deterministic>

    <!-- Used both to configure the CMake build below (-DCPPCODETOOLS_LLVM_ROOT=...) and to locate
         libclang.dll for deployment. Override with /p:LLVMInstallDir=... if LLVM isn't installed
         at the default location - same property name/default as this repo's root CMakeLists.txt
         (CPPCODETOOLS_LLVM_ROOT), kept in sync via -D below so an override actually reaches the
         native build, not just the deployment-file lookup. -->
    <LLVMInstallDir Condition="'$(LLVMInstallDir)'==''">C:\Program Files\LLVM</LLVMInstallDir>

    <!-- cpp_codetools' own repo root and CMake build directory (extension/CodeToolsVsix -> up
         two levels). NativeEditControls.dll is a CMake target (root CMakeLists.txt), built by
         invoking cmake directly against this build tree before deploying its output alongside
         this assembly - it isn't an MSBuild project and so isn't picked up by the .sln's
         ProjectDependencies ordering (indeed, it isn't in the .sln at all). -->
    <!-- No trailing backslash: one right before a closing quote in an Exec Command escapes the
         quote itself (classic Windows command-line argument parsing gotcha) and corrupts the
         next argument - observed first-hand getting the cmake build invocation below working. -->
    <RepoRoot>$(MSBuildThisFileDirectory)..\..</RepoRoot>
    <RepoBuildDir>$(RepoRoot)\build</RepoBuildDir>

    <!-- VERSION.state (repo root, tracked in git) is cpp_codetools' single source of truth for
         Major.Minor.Release.Build - shared with NativeEditControls.dll's own version (see
         cmake/GenerateVersion.cmake, NativeEditControl.rc). Read here, at project *evaluation*
         time (before any target runs) via an MSBuild property function rather than a Target-based
         file read - tried making a Target set $(Version) after BuildAndIncludeNativeDependencies'
         own native build bumps this same file (so both DLLs would share the exact same build
         number), but the SDK's CoreGenerateAssemblyInfo target didn't reliably pick up a
         Target-time property change (verified empirically: AssemblyInfo.cs kept the SDK's
         "1.0.0.0" default even across a forced /t:Rebuild), so this reads the value as of the
         START of this build instead - one build-number behind the native DLL built later in the
         same invocation, but simple and reliably correct. Both sides keep tracking the same file,
         just offset by a constant one build apart rather than resynced to the exact same instant.

         VERSION.state is a KEY=VALUE text file, one entry per line (VERSION plus a handful of
         product-metadata fields cmake/GenerateVersion.cmake also carries through unchanged into
         NativeEditControl.rc) - not a bare version string - so this pulls out just the VERSION=
         line's value via Regex rather than reading the whole file as-is. -->
    <_VersionStateText Condition="Exists('$(RepoRoot)\VERSION.state')">$([System.IO.File]::ReadAllText('$(RepoRoot)\VERSION.state'))</_VersionStateText>
    <CppCodeToolsVersion Condition="'$(_VersionStateText)' != ''">$([System.Text.RegularExpressions.Regex]::Match('$(_VersionStateText)', 'VERSION=(\S+)').Groups[1].Value)</CppCodeToolsVersion>
    <CppCodeToolsVersion Condition="'$(CppCodeToolsVersion)' == ''">0.1.0.0</CppCodeToolsVersion>
    <Version>$(CppCodeToolsVersion)</Version>
    <AssemblyVersion>$(CppCodeToolsVersion)</AssemblyVersion>
    <FileVersion>$(CppCodeToolsVersion)</FileVersion>
    <InformationalVersion>$(CppCodeToolsVersion)</InformationalVersion>
    <!-- Otherwise the SDK appends "+<git-commit-sha>" to InformationalVersion automatically
         (SourceLink-driven), making it look inconsistent next to FileVersion/AssemblyVersion. -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>

    <!-- cmake isn't on PATH in a plain shell on this machine (see repo CLAUDE.md) - default to
         VS 2026's own bundled copy rather than requiring a system-wide install. Override with
         /p:CMakeExe=... if yours is elsewhere. -->
    <CMakeExe Condition="'$(CMakeExe)'==''">$(DevEnvDir)CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe</CMakeExe>
	  <EnableUnmanagedDebugging>true</EnableUnmanagedDebugging>
  </PropertyGroup>

  <ItemGroup>
    <!-- Meta-package pulling in the VS shell / editor / OLE interop assemblies.
         Bump this to match the SDK version shipped for your installed VS build
         if VS 2026 publishes its own package line (17.14.x is current as of Aug 2026). -->
    <PackageReference Include="Microsoft.VisualStudio.SDK" Version="17.14.40265" ExcludeAssets="runtime" />
    <PackageReference Include="Microsoft.VSSDK.BuildTools" Version="18.9.820" />
  </ItemGroup>

  <ItemGroup>
    <None Include="source.extension.vsixmanifest" SubType="Designer" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Properties\Settings.Designer.cs">
      <DesignTimeSharedInput>True</DesignTimeSharedInput>
      <AutoGen>True</AutoGen>
      <DependentUpon>Settings.settings</DependentUpon>
    </Compile>
  </ItemGroup>

  <ItemGroup>
    <None Update="Properties\Settings.settings">
      <Generator>SettingsSingleFileGenerator</Generator>
      <LastGenOutput>Settings.Designer.cs</LastGenOutput>
    </None>
  </ItemGroup>

  <!-- NativeEditControls.dll (native, edit-surface HWND - a CMake target, root CMakeLists.txt)
       and libclang.dll (cpptools's own runtime dependency) are native code NativeEditHost.cs
       P/Invokes into or that NativeEditControls.dll needs alongside it. They need to sit next to
       CodeToolsVsix.dll at runtime and travel inside the .vsix, since VS loads them from the
       extension's install folder, not from this project's own build output. There is no separate
       C API/bridge DLL - NativeEditControls.dll calls cpptools::Parser in-process (see
       CppEditorControl.cpp), and hosts a real newui::RootView the same way (see
       EditThreadHost.cpp).

       This is done as an imperative Target rather than plain <Content Include> items pointing at
       the CMake build's own output path: a static Content item's Exists() condition is evaluated
       when this project is first loaded, before any build has actually happened, so on a clean
       checkout it would (wrongly) evaluate to false and silently omit the DLLs. Building
       NativeEditControls via CMake explicitly here - which transitively builds cpptools/newui too,
       since it links both - rather than relying on any MSBuild-level project ordering (there is
       none; NativeEditControls isn't in the .sln at all) - also means `dotnet build` on just this
       .csproj still produces a working, deployable extension on a machine with the VC++ toolset
       and CMake available. -->
  <Target Name="BuildAndIncludeNativeDependencies" BeforeTargets="CoreCompile;GetVsixSourceItems" Condition="'$(DesignTimeBuild)' != 'true'">
    <!-- First-time configure, mirroring the repo's own documented "cmake -S . -B build" step -
         lets a clean checkout of this repo build the extension without a manual CMake step first.
         -DCPPCODETOOLS_LLVM_ROOT keeps a /p:LLVMInstallDir=... override in sync with the actual
         native build, not just the deployment-file lookup below. -->
    <Exec Command="&quot;$(CMakeExe)&quot; -S &quot;$(RepoRoot)&quot; -B &quot;$(RepoBuildDir)&quot; -DCPPCODETOOLS_LLVM_ROOT=&quot;$(LLVMInstallDir)&quot;" Condition="!Exists('$(RepoBuildDir)\CMakeCache.txt')" />

    <!-- Builds NativeEditControls.dll - transitively builds cpptools and newui too, since it
         links both (target_link_libraries(NativeEditControls PRIVATE cpptools newui), root
         CMakeLists.txt) - one call covers everything this project needs from the native side. -->
    <Exec Command="&quot;$(CMakeExe)&quot; --build &quot;$(RepoBuildDir)&quot; --config $(Configuration) --target NativeEditControls" />

    <!-- Keeps VS's own "Manage Extensions" dialog (Identity Version, source.extension.vsixmanifest)
         in sync with the same VERSION.state this assembly's own Version/AssemblyVersion/FileVersion
         are read from (see this project's top-level PropertyGroup) - see
         UpdateVsixManifestVersion.ps1's own comment for why this is a real XML edit rather than a
         naive text replace, and why it also happens to fix VSIXInstaller.exe silently no-oping a
         reinstall (it only reinstalls when Identity Version actually changes). Must run before
         GetVsixSourceItems packages the manifest - this Target already is (BeforeTargets above). -->
    <Exec Command="powershell -NoProfile -ExecutionPolicy Bypass -File &quot;$(MSBuildProjectDirectory)\UpdateVsixManifestVersion.ps1&quot; -ManifestPath &quot;$(MSBuildProjectDirectory)\source.extension.vsixmanifest&quot; -Version &quot;$(CppCodeToolsVersion)&quot;" />

    <ItemGroup>
      <_NativeDeploymentFile Include="$(RepoBuildDir)\$(Configuration)\NativeEditControls.dll" />
      <_NativeDeploymentFile Include="$(LLVMInstallDir)\bin\libclang.dll" />
    </ItemGroup>

    <Error Condition="!Exists('$(LLVMInstallDir)\bin\libclang.dll')" Text="libclang.dll not found under LLVMInstallDir ('$(LLVMInstallDir)\bin'). Install LLVM (https://github.com/llvm/llvm-project/releases) or pass /p:LLVMInstallDir=... pointing at your install." />

    <!-- Belt-and-suspenders: a dynamically-added Content item's CopyToOutputDirectory metadata
         isn't guaranteed to be picked up by the standard output-copy pipeline (AssignTargetPaths
         may already have snapshotted @(Content) by the time this target runs), so copy into
         OutDir imperatively too - that part must not depend on pipeline-ordering nuance. -->
    <Copy SourceFiles="@(_NativeDeploymentFile)" DestinationFolder="$(OutDir)" SkipUnchangedFiles="true" />

    <ItemGroup>
      <Content Include="@(_NativeDeploymentFile)">
        <Link>%(Filename)%(Extension)</Link>
        <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
        <IncludeInVSIX>true</IncludeInVSIX>
      </Content>
    </ItemGroup>
  </Target>

</Project>
